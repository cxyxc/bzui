import { CSSProperties } from 'react';

// 普通图标
export interface BzIconsProps {
  rotate?: number;
  spin?: boolean;
  style?: CSSProperties;
}

// 文字图标
export interface BzTextIconProps {
  text: string;
}
